#!/usr/bin/env ruby

require "bundler/setup"
require "grizzly"
require 'byebug'
require_relative "../cops/cops"

# You can add fixtures and/or initialization code here to make experimenting
# with your gem easier. You can also use a different console, if you like.

if ARGV[0]
  `rubocop --only Style/ArrayInitialization --autocorrect spec/ruby/core/array`
else
  `rubocop --only Style/ArrayInitialization spec/ruby/core/array`
end
